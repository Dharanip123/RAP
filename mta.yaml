_schema-version: "3.2"
ID: travustdptravelproc
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: travustdptravelproc-destination-content
  type: com.sap.application.content
  requires:
  - name: travustdptravelproc-destination-service
    parameters:
      content-target: true
  - name: travustdptravelproc-repo-host
    parameters:
      service-key:
        name: travustdptravelproc-repo-host-key
  - name: travustdptravelproc-uaa
    parameters:
      service-key:
        name: travustdptravelproc-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: travustdptravelproc_html_repo_host
          ServiceInstanceName: travustdptravelproc-html5-srv
          ServiceKeyName: travustdptravelproc-repo-host-key
          sap.cloud.service: travustdptravelproc
        - Authentication: OAuth2UserTokenExchange
          Name: travustdptravelproc_uaa
          ServiceInstanceName: travustdptravelproc-xsuaa-srv
          ServiceKeyName: travustdptravelproc-uaa-key
          sap.cloud.service: travustdptravelproc
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: travustdptravelproc-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: travustdptravelproc-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - travustdptravelproc.zip
      name: travustdptravelproc
      target-path: resources/
- name: travustdptravelproc
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: travustdptravelproc-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: travustdptravelproc-destination-service
    service-plan: lite
- name: travustdptravelproc-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: travustdptravelproc-xsuaa-srv
    service-plan: application
- name: travustdptravelproc-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: travustdptravelproc-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
